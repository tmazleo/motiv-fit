import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    lgo: {
        color: "#4EC668",
        fontSize: 40
    },
    lgo2: {
        color: "#ffffff",
    }
});

export default styles;